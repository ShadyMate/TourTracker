{
  "properties": [
    {
      "name": "app.jwt.secret",
      "type": "java.lang.String",
      "description": "Base64-encoded HMAC-SHA256 signing key for JWT generation and validation."
    },
    {
      "name": "app.jwt.expiration-ms",
      "type": "java.lang.Long",
      "description": "JWT token validity in milliseconds (default: 86400000 = 24 hours)."
    }
  ]
}
